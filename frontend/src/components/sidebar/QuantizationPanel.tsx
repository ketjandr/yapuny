// QuantizationPanel: quantize all / per-node (w8/w4). Placeholder.
export function QuantizationPanel() {
  return (
    <section className="grp">
      <h3>
        Quantization
        <span className="tag">soon</span>
      </h3>
      {/* TODO: per-node w8/w4 toggles; inference-only (visual-only in train mode) */}
    </section>
  );
}
