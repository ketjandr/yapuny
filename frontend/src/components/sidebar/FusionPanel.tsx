// FusionPanel: fuse/unfuse contiguous nodes. Placeholder.
export function FusionPanel() {
  return (
    <section className="grp">
      <h3>
        Fusion
        <span className="tag">soon</span>
      </h3>
      {/* TODO: fuse/unfuse selected group; visual-only in train mode (keep in sent graph) */}
    </section>
  );
}
