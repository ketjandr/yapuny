// GenerationPanel: prompt, streaming output, sampling params. Placeholder.
export function GenerationPanel() {
  return (
    <section className="grp">
      <h3>Generate</h3>
      {/* TODO: prompt (string) -> /generate/stream (SSE) token append; enabled only when compiled+trained */}
    </section>
  );
}
