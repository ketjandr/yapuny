// BenchmarkPanel: tokens/sec, per-node latency, before/after compare. Placeholder.
export function BenchmarkPanel() {
  return (
    <section className="grp">
      <h3>Benchmark</h3>
      {/* TODO: /bench/generate (multi-model compare by graph_idx) + /bench/profile (per-node) */}
    </section>
  );
}
