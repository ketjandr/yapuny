// TrainingPanel: live loss, Train/Stop, corpus. Placeholder.
export function TrainingPanel() {
  return (
    <section className="pan">
      <h3 className="side-eyebrow">Training</h3>
      {/* TODO: corpus upload/prepare (/data/*); Train -> /train/stream (SSE) live loss; Stop -> /train/stop */}
    </section>
  );
}
